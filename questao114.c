#include <stdio.h>

int main()
{
    int curupira, boitata, boto, mapinguari, iara;
    scanf("%d %d %d %d %d", &curupira, &boitata, &boto, &mapinguari, &iara);
    printf("%d\n", 300*curupira + 1500*boitata + 600*boto + 1000*mapinguari + 150*iara + 225);
    return 0;
}